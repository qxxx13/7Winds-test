export const createDataEdit = (
    level: JSX.Element,
    name: JSX.Element,
    selary: JSX.Element,
    oborudovanie: JSX.Element,
    nakladnie: JSX.Element,
    pribil: JSX.Element,
    id: number,
) => {
    return { level, name, selary, oborudovanie, nakladnie, pribil, id };
};
