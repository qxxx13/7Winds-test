export const createData = (level: JSX.Element, name: string, selary: number, oborudovanie: number, nakladnie: number, pribil: number, id: number) => {
    return { level, name, selary, oborudovanie, nakladnie, pribil, id };
};
